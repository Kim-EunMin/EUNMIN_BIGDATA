#HTML 구조

# <HTML>  -> tag 시작  (tag 안에는 attribute 들어감)
#   <HEAD>-> 기본 정보만 보여줌
#   </HEAD>
#    <BODY>  -> web brower 가 표시된다.
#       <HI>-</H1>
#     </BODY>
# </HTML>  -> tag 끝

#web scraping 은 webbrower 에 표시된 내용을 가져오는 것이다. 
#내가 원하는 텍스트를 다운받을 수 없을때? web scraping web 크롤링 같은 방법을 쓰는 것이다. 

